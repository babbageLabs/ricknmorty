export function SearchBar({
  onSubmit,
}: {
  children: React.ReactNode;
  className?: string;
}): JSX.Element {
  return (
    <div className="pt-2 relative mx-auto text-gray-600">

    </div>
  );
}
